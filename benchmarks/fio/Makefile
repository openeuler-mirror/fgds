
THIS_MAKEFILE_DIR := $(dir $(abspath $(lastword $(MAKEFILE_LIST))))
ROOT_DIR := $(abspath $(THIS_MAKEFILE_DIR)/../..)

FGDS_INCLUDE_DIR := ${ROOT_DIR}/libfgds/include
FGDS_LIB_DIR := ${ROOT_DIR}/build
FIO_SRC_DIR ?= ${ROOT_DIR}/third-party/fio
CUDA_HOME ?= /usr/local/cuda
CUDA_LIB_DIR := ${CUDA_HOME}/lib64
CUDA_INC_DIR := ${CUDA_HOME}/include

PLUGIN_NAME = fgds_fio
SO_NAME = ${PLUGIN_NAME}.so
SRC = ${PLUGIN_NAME}.cpp
OBJ = ${PLUGIN_NAME}.o

CXX = g++
CXXFLAGS = -fPIC -fpermissive -O3 -D_GNU_SOURCE -shared -rdynamic -I${FGDS_INCLUDE_DIR} -I${FIO_SRC_DIR} -I${CUDA_INC_DIR} -include ${ROOT_DIR}/third-party/fio/config-host.h
LDFLAGS = -L${FGDS_LIB_DIR} -lfgds -Wl,-rpath=${FGDS_LIB_DIR} -L${CUDA_LIB_DIR} -lcudart -luring

.PHONY: all clean

all: ${SO_NAME}

${SO_NAME}: ${SRC}
	@echo "Building shared object: ${ROOT_DIR}"
	${CXX} ${CXXFLAGS} $^ -o $@ ${LDFLAGS}

clean:
	rm -rf ${OBJ} ${SO_NAME}
